import { styled } from '@mui/material/styles';

export const Wrapper = styled('div')`
  .tweetIconsContainer {
    display: flex;
    justify-content: space-between;
    margin-left: 25;
    margin-right: 25;
  }
`;
