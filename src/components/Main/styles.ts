import { styled } from '@mui/material/styles';

export const Wrapper = styled('div')`
  .main-title {
    padding: 1rem;
    position: fixed;
    top: 0;
    width: 54.5%;
    background: white;
    z-index: 1;
  }
  .createTweetFormWrapper {
    margin-top: 50px;
  }
`;
